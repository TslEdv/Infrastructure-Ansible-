---
  - name: Setup
    hosts: all
    become: yes
    gather_facts: yes
    tasks:
      - setup:

  - name: Init
    hosts: all
    become: yes
    roles:
      - init

  - name: Database server
    hosts: db_servers
    become: yes
    roles:
      - mysql
      - bind

  - name: Web server
    hosts: web_servers
    become: yes
    roles:
      - agama
      - uwsgi
      - nginx

  - name: Config DNS
    hosts: all
    become: yes
    roles:
      - dns
